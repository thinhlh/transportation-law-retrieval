version: '3'

services:
  db: 
    image: postgres
    container_name: law-modeling-db
    ports:
      - 5432:5432
    env_file:
      - ./env/dev.env

  cache:
    image: redis
    container_name: law-modeling-cache
    ports:
      - 6379:6379